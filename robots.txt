User-agent: *
Allow: /

# Preview mirror for client review only.
# Every page carries <meta name="robots" content="noindex, nofollow">.
# Crawling stays allowed so that tag is actually seen. Do not add a Disallow:
# blocking a URL prevents Google reading the noindex and it can stay indexed.
